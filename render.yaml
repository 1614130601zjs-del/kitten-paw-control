services:
  - type: web
    name: kitten-paw-control
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn toy_server_kitten:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CACHITO_ACCOUNT_ID
        sync: false
