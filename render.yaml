services:
  - type: web
    name: kitten-paw-control
    runtime: python
    plan: free                 # 👈 允许休眠
    buildCommand: pip install -r requirements.txt
    startCommand: python toy_server_kitten.py
    envVars:
      - key: CACHITO_ACCOUNT_ID
        sync: false
